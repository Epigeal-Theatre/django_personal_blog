'''
this should be the standard way of making urls then pasing them to...
the project scope urls.py but we hold off for now


from django.urls import path
from . import views

urlpatterns = [
    #path('', views.home, name='blog-home')

'''